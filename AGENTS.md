# このリポジトリを変更するときの決まりごと

利用者向けの情報は [README.md](README.md)。ここは実装と保守のための索引と禁止事項で、
理由を持たない。理由はコード側（`slk` の冒頭 docstring と各関数の docstring）にある。

## 権威の所在

- **Slack API の癖**（知らないと必ず一度踏むもの）は `slk` の冒頭 docstring が権威。ここに写さない
- **コマンドの一覧と引数**は `slk` の `USAGE` が権威。README のコマンド一覧は読み物なので、
  食い違ったら `USAGE` が正しい

## public repo なので入れてはいけないもの

git 履歴は force-push しても GitHub 上に SHA で残る。入れてしまったら消せないと考えること。

- 取得したメッセージ・ログ。作業ディレクトリをこのリポジトリにしない
- 実在のワークスペース URL・チャンネル名・実名。ドキュメントの例はすべてダミーにする
  （チャンネル名は組織構造と顧客名が出る）
- ローカルのディレクトリ構成。インストール手順で clone 先を名指ししない
- トークンと `client_secret`

上のうち機械で防げるものは `.githooks/pre-commit` が止める（`git config core.hooksPath .githooks`）。

## 変更してはいけないもの

知らない人が見ると、逆向きに変えるほうが素直な改善に見えるものだけを挙げる。

- **`READ_METHODS` に書き込みメソッドを足さない。** manifest が書き込み scope を要求しないことと
  この allowlist の 2 点だけが read-only を担保している。HTTP メソッドは境界にならない
  （`chat.postMessage` も GET で通る）
- **`oauth.v2.access` を `READ_METHODS` に入れない。** 統一されて見えるが、allowlist を
  「読み取り以外の入口」にしてしまう。OAuth はデータを読まないので別扱いのままにする
- **`docs/` 配下に内部向けの文書を置かない。** ここは GitHub Pages の公開ディレクトリで、
  置いたものはそのまま公開される
- **着地ページ（`docs/callback/`）に外部リソースを読み込まない。** CSS ひとつでも Referer 経由で
  認可コードが第三者に渡る。CSS と JS はあのファイルに閉じる
- **`app.json` の探索順を「リポジトリ優先」に戻さない。** リポジトリ側は配布される既定値なので、
  `~/.config/slk/app.json` の個人設定が勝つ向きでないと上書きできなくなる
- **キャッシュをプロファイル間で共有しない。** `U…` / `C…` はワークスペース内でしか意味を持たず、
  共有するとエラーにならずに別人の名前で表示される
- **Keychain の項目を `client_id` で分けるのをやめない。** アプリを差し替えたときに前の
  `client_secret` を掴む

## 試すときに踏むもの

- Keychain の項目は `(service, account)` だけが鍵で、`SLK_CONFIG_DIR` は鍵に入らない。
  試験用の設定ディレクトリを作るときは `SLK_KEYCHAIN_SERVICE` も一緒に変えないと、
  本物のトークンを引く
