# このリポジトリを変更するときの決まりごと

利用者向けの情報は [README.md](README.md) と各ツールの `<tool>/README.md`。
ここは実装と保守のための索引と禁止事項で、理由を持たない。理由はコード側
（各ツール冒頭の docstring と関数の docstring）にある。

## 構成

ツール 1 つにつき 1 ディレクトリ。`<tool>/<tool>` が実行ファイル本体で、同じ場所に
その README と（必要なら）設定のひな型を置く。GitHub Pages 用の静的ページは
`docs/<tool>/` に置く。

## 権威の所在

- **API の癖**（知らないと必ず一度踏むもの）は各ツール冒頭の docstring が権威。ここに写さない
- **コマンドの一覧と引数**は各ツールの `--help` / `USAGE` が権威。README のコマンド一覧は
  読み物なので、食い違ったら `USAGE` が正しい

## public repo なので入れてはいけないもの

git 履歴は force-push しても GitHub 上に SHA で残る。入れてしまったら消せないと考えること。

- 取得したメッセージ・ログ。作業ディレクトリをこのリポジトリにしない
- 実在のワークスペース URL・チャンネル名・実名。ドキュメントの例はすべてダミーにする
  （チャンネル名は組織構造と顧客名が出る）
- ローカルのディレクトリ構成。インストール手順で clone 先を名指ししない
- トークンと `client_secret`

上のうち機械で防げるものは `.githooks/pre-commit` が止める（`git config core.hooksPath .githooks`）。

## 変更してはいけないもの

知らない人が見ると、逆向きに変えるほうが素直な改善に見えるものだけを挙げる。

すべてのツールに共通:

- **認証情報を付けて任意のホストを叩けるようにしない。** 引数の絶対 URL は、設定した
  そのサービスのオリジンと突き合わせてから使う。**リダイレクトも追わない**
  （urllib は転送先が別ホストでも Authorization を持ち回るので、入口だけ確認しても漏れる）。
  実装例は `tldv` と `zoho` の `api_url()` / `_NoRedirect`
- **トークンを取りに行く要求も `urlopen` で投げない。** 本文に入れた `client_secret` は
  302 で POST→GET になって落ちるが、`Authorization` ヘッダは転送先まで付いて行く。
  ヘッダに資格情報を載せるなら `_NoRedirect` を挿した opener を使う（`mf` と `zp`）
- **取り切れなかった一覧を exit 0 で返さない。** 途中までの JSON を 0 で返すと
  `--all > out.json` が完全な取得と区別できない。知らせを stderr にだけ出すのは
  「直した」ことにならない（リダイレクトで消える）。カーソルや総件数は出力に残したまま
  exit 1 にする。件数を自分で数え直して総数として書き直すのは特に駄目で、
  打ち切った数が正しい総数に見える
- **一覧の配列キーを JSON の並び順で選ばない。** 候補が複数あるとき先頭を取ると、
  関係ない配列を伸ばして本命を 1 ページ目のまま返す。決められないなら決めずに倒す
- **ページングのループにトークン／カーソルの再出現の検出を必ず置く。** 同じ値を返し続ける
  API があると件数では終端条件にならず、止まらない
- **疎通確認コマンドを exit 0 に戻さない。** `… health && 次の処理` が
  壊れた資格情報のまま先へ進む
- **秘密ファイルを `open()` してから `chmod()` しない。** その間は umask の権限
  （通常 0644）で他人に読める。`os.open(..., O_CREAT|O_EXCL|O_WRONLY, 0o600)` で
  作成時に確定させる。config dir も `makedirs(mode=0o700)` で作り、**読み込みのたびに
  締め直す**（`zp` / `tldv` / `mf` / `zoho` の `secure()`）。ファイルだけ閉じても、
  ディレクトリが書けるなら中身ごと差し替えられる
- **`init` は config dir を作ってから書く。** 作らないと新しいマシンで
  README の手順 1 が `FileNotFoundError` で落ちる
- **`HTTPError` は `URLError` の派生。** `except` の順序を入れ替えると 3xx と 401 の
  処理が死ぬ。`URLError` を捕まえないまま放置するのも駄目（DNS 断がトレースバックになる）
- **200 が JSON とは限らない。** キャプティブポータル・プロキシ・WAF は 200 で HTML を返す。
  `json.loads` を素で呼ばない

slk について:

- **`READ_METHODS` に書き込みメソッドを足さない。** manifest が書き込み scope を要求しないことと
  この allowlist の 2 点だけが read-only を担保している。HTTP メソッドは境界にならない
  （`chat.postMessage` も GET で通る）
- **`oauth.v2.access` を `READ_METHODS` に入れない。** 統一されて見えるが、allowlist を
  「読み取り以外の入口」にしてしまう。OAuth はデータを読まないので別扱いのままにする
- **`docs/` 配下に内部向けの文書を置かない。** ここは GitHub Pages の公開ディレクトリで、
  置いたものはそのまま公開される
- **着地ページ（`docs/slk/callback/`）に外部リソースを読み込まない。** CSS ひとつでも Referer 経由で
  認可コードが第三者に渡る。CSS と JS はあのファイルに閉じる
- **`app.json` の探索順を「リポジトリ優先」に戻さない。** リポジトリ側は配布される既定値なので、
  `~/.config/slk/app.json` の個人設定が勝つ向きでないと上書きできなくなる
- **キャッシュをプロファイル間で共有しない。** `U…` / `C…` はワークスペース内でしか意味を持たず、
  共有するとエラーにならずに別人の名前で表示される
- **ファイルの実体 URL を引数から直接叩かない。** 渡された値は ID に落として `files.info` に
  問い直し、**API が返した URL だけ**を、ホスト（`FILE_HOST_SUFFIX`）を確認してから叩く
- **転送を追うときに `Authorization` を持ち回らない。** 配信は署名付き URL に転送されることが
  あり、付けたままだとトークンが CDN へ渡る（`fetch_binary`）
- **保存先の存在確認と書き込みを分けない。** 確認から書き終わるまでの間に作られたファイルを
  潰す。`open(dest, "xb")` で名前の確保ごと不可分にし、`--force` のときだけ一時ファイル経由で
  置き換える（ダウンロードが失敗しても既存を消さないため）。名前の確保は
  `os.open(..., O_CREAT|O_EXCL|O_WRONLY, 0o600)` で行い、権限を `--force` の有無で変えない
- **待ち受けが受け取った認可応答の `state` 検証を緩めない。** 貼り付け経路（人間が code
  だけを貼る）だけが warn で通ってよい。待ち受けは誰でも叩けるので、そこを緩めると
  他人の認可コードを流し込まれ、`client_secret` を持つこちら側が交換してしまう
  （`error=` だけ投げればログイン妨害もできる）
- **read-only の門を `_verb` より後ろに置かない。** `_verb` が真なら allowlist を
  素通りする形にすると、`_verb` を渡す呼び出しが 1 つ増えただけで保証が消える
- **プロファイルの同一性を `team_id` だけで判定しない。** 同じワークスペースの別アカウントで
  ログインしたときに、他方のトークンを黙って上書きする。判定は `(team_id, user_id)` の組
- **Keychain の項目を `client_id` で分けるのをやめない。** アプリを差し替えたときに前の
  `client_secret` を掴む
- **ツール間で共通モジュールを切り出さない。** 重複は見えるが、「1 ファイルをコピーすれば動く・
  依存パッケージが無い」がこのリポジトリの唯一の売りで、共有モジュールを挟むとそれが消える

gws について:

- **`[ 条件 ] && echo …` で分岐を終えない。** 条件が偽のときその 1 が関数の戻り値になり、
  `set -e` が**成功時に** exit 1 させる。`if … then … fi` で書く
- **`GOOGLE_WORKSPACE_CLI_{TOKEN,CREDENTIALS_FILE,CLIENT_ID,CLIENT_SECRET}` を素通ししない。**
  本体はこれらをプロファイルより優先する。残っていると `profile current` が work と
  言いながら別 ID で叩く——このずれを防ぐことがこの wrapper の存在理由
- **有効プロファイルの判定に `current_profile` を使わない。** `GWS_PROFILE` を優先して
  返すので、`profile rm` のガードに使うと active に書かれている方を消せてしまう。
  ガードは `ACTIVE_FILE` を直接読む
- **自分自身の検出をパス文字列で比較しない。** 同じ wrapper への symlink が PATH に
  2 つあると互いを別物と見なし、`exec` が往復して止まらない。実体（`stat -f '%d:%i'`）で見る
- **`first_email` の `|| true` を外さない。** `pipefail` が有効なので、`grep` の不一致と
  `head` による SIGPIPE がそのまま呼び出し側の `set -e` に伝わって落ちる

## 試すときに踏むもの

- Keychain の項目は `(service, account)` だけが鍵で、`SLK_CONFIG_DIR` は鍵に入らない。
  試験用の設定ディレクトリを作るときは `SLK_KEYCHAIN_SERVICE` も一緒に変えないと、
  本物のトークンを引く
- `gws` の Keychain も同じで、鍵は (service=`gws-cli`, account=**OS のユーザー名**) だけ。
  config dir は鍵に入らないので、`credentials.enc` はプロファイルごとでも**復号鍵は 1 つ**。
  プロファイルは利便性の境界であってセキュリティ境界ではない
  （バイナリへの ACL 許可 1 回で全プロファイルが復号できる）。
  なお `gws auth logout` は config dir のファイルを消すだけで**鍵には触れない**ので、
  他のプロファイルは巻き添えにならない（2026-09-15 に実測。ダミーの `credentials.enc` を
  置いた一時ディレクトリに対して実行し、応答の `removed` がそのファイルだけを挙げること、
  項目の `mdat` が動かないこと、他プロファイルが復号できることを確認した）
